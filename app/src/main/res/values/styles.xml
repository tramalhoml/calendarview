<resources>
    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="Theme.Custom" parent="@android:style/Theme">
        <item name="android:calendarViewStyle">@style/Widget.CalendarView.Custom</item>
    </style>

    <style name="Widget.CalendarView.Custom" parent="android:Widget.CalendarView">
        <item name="android:focusedMonthDateColor">@color/colorPrimary</item>
        <item name="android:weekDayTextAppearance">@style/CalenderViewDateCustomText</item>
        <item name="android:dateTextAppearance">@style/CalenderViewDateCustomText</item>
        <item name="android:textAppearance">@style/CalenderViewDateCustomText</item>
    </style>

    <style name="CalenderViewDateCustomText" parent="android:TextAppearance">
        <item name="android:textColor">@android:color/black</item>
        <item name="android:fontFamily">@font/lobster_two_italic</item>
        <item name="fontFamily">@font/lobster_two_italic</item>
    </style>

</resources>